{
  "name": "secureshare",
  "private": true,
  "version": "1.0.0",
  "description": "SecureShare full-stack monorepo",
  "scripts": {
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build:client": "npm --prefix client run build",
    "seed:admin": "npm --prefix server run seed:admin"
  }
}
